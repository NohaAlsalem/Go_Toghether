class internalreservationpost{

  String ? driverid;
  String ? location;
  String ? goal;
  String ? traveltime;
  String ? date;
  String ? type_book;
  String ? num_person;
  String ? content;

  internalreservationpost({
 this.location,
    this.date,
    this.goal,
    this.driverid,
    this.traveltime,
    this.content,
    this.num_person,
    this.type_book,

  });

}