class driverrequist{
  String ? phoneone;
  String ? phonetwo;
  String ? password;
  String ? email;
  String  ? firstname;
  String ? lastname;
  String ? image_driver;
  String ? image_agency;
  String ? address;
  String  ? date_of_birth;
  driverrequist({
   this.email,
    this.password,
    this.address,
    this.date_of_birth,
    this.firstname,
    this.image_agency,
    this.image_driver,
    this.lastname,
    this.phoneone,
    this.phonetwo,
  });

}